
AppFabric Shell => plugins

- every plugin builds independently, deploys independently
- loads dynamically at runtime in quickbook

 - in plugin 
	 - having config.json for plugin defination
	 - having widget id, routes,dependencies, feature flags in config.json

- Two main bild systems are used
	- Plugin-cli
	- mercury-cli